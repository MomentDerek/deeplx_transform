# Docker Compose 环境变量配置示例
# 复制此文件为 .env 并修改相应的值

# 服务端口映射
HOST_PORT=8080
CONTAINER_PORT=8080

# Gin 框架模式
GIN_MODE=release

# 目标服务配置
TARGET_BASE_URL=https://api.deeplx.org

# 代理配置（如果需要）
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# 容器名称
CONTAINER_NAME=deeplx-transform

# 重启策略
RESTART_POLICY=unless-stopped